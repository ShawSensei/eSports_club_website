Run the following command to regenerate TypeScript types from the local Supabase schema, then confirm the output file was updated:

```bash
supabase gen types typescript --local > src/types/supabase.ts
```

After running, briefly summarise any new or changed types that are relevant to the current task.
