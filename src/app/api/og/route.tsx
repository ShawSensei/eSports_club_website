// Open Graph image generation — built out in Phase 11.
import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'OG image generation — Phase 11' })
}
